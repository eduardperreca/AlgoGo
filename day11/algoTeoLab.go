package main

func main() {
	// se ho un elemento ordinato creando spazio e spostando tutto per dare spazio all'elemento nuovo.
	// costo: O(n) se ho spazio, O(n) se voglio riallocare tutto in un nuovo vettore O(n), nelle liste é O(1).

	// questa cosa si puó abbreviare cosí:
	// stronzate col copy

	// albero binario di ricerca
	// ogni nodo ha al piú 2 figli
	// ogni nodo ha un valore
}
